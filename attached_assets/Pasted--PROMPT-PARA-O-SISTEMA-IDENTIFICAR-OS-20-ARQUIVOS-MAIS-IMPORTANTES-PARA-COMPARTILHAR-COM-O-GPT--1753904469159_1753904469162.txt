**PROMPT PARA O SISTEMA IDENTIFICAR OS 20 ARQUIVOS MAIS IMPORTANTES PARA COMPARTILHAR COM O GPT**

Objetivo:
Identificar automaticamente os 20 arquivos mais importantes e relevantes do sistema de Dashboard Financeiro, com foco em arquivos que contenham configurações, filtros, lógica de inteligência e elementos de interface, para integração com o modelo GPT.

Critérios de Seleção:

1. **Tipos de arquivos alvo:**

   * .json, .xml, .config (Configurações e parâmetros do sistema)
   * .cs (Código C# com lógica de negócio e regras de inteligência)
   * .cshtml, .js, .css (Elementos visuais, filtros e interação com o usuário)

2. **Critérios de importância:**

   * Frequência de uso pelo sistema (métricas de acesso ou chamadas)
   * Data de modificação recente (ultimos 90 dias)
   * Densidade de código ou configurações (linhas de código, número de chaves ou elementos)
   * Relacionamento direto com funcionalidades críticas do dashboard (filtros, relatórios, cálculos financeiros, gráficos)

3. **Regras de exclusão:**

   * Ignorar arquivos de teste, logs, binários e backups.
   * Ignorar arquivos duplicados ou obsoletos.

4. **Execução esperada:**

   * Escanear recursivamente o diretório principal do projeto.
   * Avaliar cada arquivo com base nos critérios acima.
   * Selecionar os 20 arquivos com maior relevância.
   * Gerar um arquivo `arquivos_importantes_para_gpt.txt` com a seguinte estrutura:

```
Caminho: C:\Projeto\Dashboard\Config\appsettings.json
Importância: Contém todas as configurações de conexão e variáveis globais do sistema.

Caminho: C:\Projeto\Dashboard\Negocio\RelatorioFinanceiroService.cs
Importância: Classe principal responsável pela geração dos relatórios financeiros.

Caminho: C:\Projeto\Dashboard\UI\Filtros\FiltroPeriodo.cshtml
Importância: Interface visual do filtro de período utilizado em todos os relatórios.

...
```

Notas finais:

* O processo deve ser 100% automatizado.
* A saída final deve conter **exatamente 20 arquivos**, priorizando os mais críticos para compreensão geral do funcionamento do dashboard financeiro.
